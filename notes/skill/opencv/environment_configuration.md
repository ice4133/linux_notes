# 安装核心依赖与opencv

```
sudo apt install build-essential cmake git pkg-config -y

sudo apt install libopencv-dev -y

```

验证安装
```
gcc --version
g++ --version

make --version
cmake --version

pkg-config --version

```



# 配置vscode 开发环境
目的是让vscode从一个单纯的编辑器变成一个可以智能感知opencv的ide


配置C++即可