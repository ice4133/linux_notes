阅读顺序：

motor_init.py
// motor_init2.py和1.py不用管，和motor_init.py是一样的

┌─────────────────────────────────────────────────────────────┐
│           imu_launch.py (启动协调器)                         │
└──────────────────────┬──────────────────────────────────────┘
                       │
       ┌───────────────┼───────────────┐
       ▼               ▼               ▼
   
┌─────────────────┐  ┌──────────────┐  ┌──────────────────┐
│ unitree_go_1.py │  │MID360雷达    │  │imu_process_node  │
│  (主控制器)      │  │(环境感知)     │  └──────────────────┘
└────────┬─────────┘  └──────────────┘         ▲
         │                                      │
         │◄─────────── ROS话题发布/订阅 ─────┘
         │
         ├─► upper_comms_node ──► 上位机JSON指令
         │                        (识别障碍/二维码)
         │
         ├─► decision_control ──► 目标速度计算
         │   (决策PID)
         │
         ├─► manual.py ─► 步态库查表
         │   (步态状态机)
         │
         ▼
    ┌─────────────────────┐
    │  node_pub.py        │ ROS Float32MultiArray
    │ (电机命令发布器)     │
    └──────┬──────────────┘
           │
           ▼
    ┌────────────────────┐
    │ serial_node.py     │ 串口字节流
    │(串口通信驱动)      │
    └──────┬─────────────┘
           │
           ▼
      ┌─────────────┐
      │  Unitree    │ 硬件层
      │ 12个关节电机│ (Go1机械臂)
      └─────────────┘