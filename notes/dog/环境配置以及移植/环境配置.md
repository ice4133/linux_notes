# 给主机配环境

## 装ubuntu

## 配ros2


## 配python

## 配一些其余库
缺少 tf_transformations库，安装方法如下：
sudo apt update
sudo apt install ros-humble-tf-transformations
//还需要安装它的底层依赖
pip3 install transforms3d



缺少keyboard
pip3 install keyboard
报错不用管，是故意让它报错的


import serial出错

输入pip3 install pyserial
只是在调用的时候叫serial，实际上下载的是pyserial这个库，安装完成后就可以正常导入了