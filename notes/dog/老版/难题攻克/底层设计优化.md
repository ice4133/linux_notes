引入ros2_control


增加“心跳”检测 (Watchdog)：


参数服务 (Parameters)


实时调度

用pthread来管理节点的实时调度


先以独立节点开发： 这样调试最直观，VS Code 监控每个节点的 Topic 也很方便。

后期性能优化再组件化： 只要你现在写代码时，坚持每个节点都是一个独立的 Class（而不是把所有东西写在 main 函数里），未来只需要改几行代码，就能把它们合并成 Component。