# 需要解决的问题
有人给我发第一张图里面的文件，可是我没有办法把它整理出一个可以供mujoco使用的stl文件。于是有人给我发总装的，但是因为有螺丝那些的，mujoco又无法打开。于是我就想把总装的文件里面的螺丝那些去掉，留下一个可以供mujoco使用的stl文件。


第一步安装blender
```bash

sudo apt update
sudo apt install blender
```

首先学会使用mujoco，它需要什么
对于 MuJoCo 仿真来说，描述一个机器人就像是在构建一个“数字生物”。你可以将其核心需求归纳为 “骨骼、关节、皮肤、肌肉、感官” 这五个逻辑类别