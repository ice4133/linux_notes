我觉得首先要知道自己的配置是怎样的，如何让自己的配置能够在服务器上跑

首先是公司服务器确实有conda ，以及配置sonic_z环境

对于sonic这个代码
所需的环境如下：









服务器环境弄好了之后
把代码上传
```bash
cd /home/ice

zip -r sonic.zip sonic \
  -x "sonic/.pytest_cache/*" \
     "sonic/**/__pycache__/*" \
     "sonic/*.pyc" \
     "sonic/logs_rl/*" \
     "sonic/logs_eval/*" \
     "sonic/outputs/*"

```

scp /home/ice/sonic.zip root@xxx.xxx.xxx.xxx:/root/xuxiangling/
和vpn有一定关系，换一个好一点的vpn，但是关系也不大





然后我发现一个问题，工程里的配置，我其实也改了，比如pyproject.toml里面的numpy和wandb
ai说没有关系




还有就是为了适配我自己的主机才写的两个sh，放到服务器会不会出问题

ai说不会

上传之后，先做测试


小规模冒烟实验完整命令
```bash

cd /root/xuxiangling/sonic

SONIC_REPO=/root/xuxiangling/sonic \
SONIC_CONDA_ENV=sonic_z \
CONDA_SH=/root/miniconda3/etc/profile.d/conda.sh \
NNODES=1 \
NODE_RANK=0 \
NPROC=1 \
RANK0_IP=127.0.0.1 \
MASTER_ADDR=127.0.0.1 \
MASTER_PORT=29631 \
CUDA_VISIBLE_DEVICES=0 \
NUM_ENVS=16 \
NUM_STEPS_PER_ENV=8 \
EXP_VAR=sonic_wpc_smoke \
CHECKPOINT=/root/xuxiangling/sonic/sonic_release/last_wpc.pt \
CHECKPOINT_ROOT=/root/xuxiangling/sonic/training_checkpoints \
MOTION_FILE=/root/xuxiangling/sonic/sample_data/robot_filtered \
SMPL_MOTION_FILE=/root/xuxiangling/sonic/sample_data/smpl_filtered \
USE_WANDB=False \
WANDB_MODE=disabled \
./sonic_launch/heft_wpc/run_sonic_wpc_rank0.sh \
++algo.config.num_learning_iterations=5 \
'++manager_env.events.window_payload.params.cap_curriculum_scale_range=[1.0,1.0]' \
++manager_env.events.window_payload.params.window_no_load_ratio=0.0 \
++callbacks.model_save.save_frequency=5 \
++callbacks.model_save.save_last_frequency=1
```

检查
find "$SONIC_REPO/training_checkpoints/sonic_wpc_smoke" \
  -maxdepth 1 -type f -printf '%f  %s bytes\n' | sort


预期至少有
```text

config.yaml
meta.yaml
last.pt
model_step_000005.pt
```


然后实际读取
```python
python - <<'PY'
import torch

path = "training_checkpoints/sonic_wpc_smoke/model_step_000005.pt"
checkpoint = torch.load(path, map_location="cpu", weights_only=False)

print("[OK] 冒烟checkpoint能够读取")
print("字段：", sorted(checkpoint.keys()))
print("训练步数：", checkpoint["state"].global_step)
PY


"""
这一步实际证明
WPC在真实 Isaac Lab仿真中能够进入训练循环，并产生新的模型
"""
```




有负载小规模评估
```bash

# 先运行conda环境，并且进入sonic
SONIC_REPO=/root/xuxiangling/sonic \
SONIC_CONDA_ENV=sonic_z \
CONDA_SH=/root/miniconda3/etc/profile.d/conda.sh \
CHECKPOINT=/root/xuxiangling/sonic/training_checkpoints/sonic_wpc_smoke/model_step_000005.pt \
MOTION_FILE=/root/xuxiangling/sonic/sample_data/robot_filtered \
SMPL_MOTION_FILE=/root/xuxiangling/sonic/sample_data/smpl_filtered \
CUDA_VISIBLE_DEVICES=0 \
NUM_ENVS=16 \
MAX_UNIQUE_MOTIONS=16 \
WPC_EVAL=full \
EVAL_NAME=smoke_with_load \
RENDER=false \
HEADLESS=true \
WANDB_MODE=disabled \
./sonic_launch/heft_wpc/run_sonic_wpc_eval_local.sh

```
/root/xuxiangling/sonic/training_checkpoints/sonic_wpc_smoke/
eval_metrics_smoke_with_load_model_step_000005/


Env/wpc/mean_load_kg
Env/wpc/max_load_kg
Env/wpc/mean_hand_force_n
Env/wpc/frac_no_force
Env/wpc/frac_ramp
Env/wpc/frac_hold

有负载时，mean_hand_force_n 应该大于0。




正式测试
```bash


cd /root/xuxiangling/sonic

SONIC_REPO=/root/xuxiangling/sonic \
SONIC_CONDA_ENV=sonic_z \
CONDA_SH=/root/miniconda3/etc/profile.d/conda.sh \
NNODES=1 \
NODE_RANK=0 \
NPROC=8 \
RANK0_IP=127.0.0.1 \
MASTER_ADDR=127.0.0.1 \
MASTER_PORT=29633 \
CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7 \
NUM_ENVS=4096 \
NUM_STEPS_PER_ENV=24 \
EXP_VAR=sonic_wpc_server_001 \
CHECKPOINT=/root/xuxiangling/sonic/sonic_release/last_wpc.pt \
CHECKPOINT_ROOT=/root/xuxiangling/sonic/training_checkpoints \
MOTION_FILE=/mnt/nfs/humanoid/sonic/robot_filtered \
SMPL_MOTION_FILE=/mnt/nfs/humanoid/sonic/smpl_filtered \
USE_WANDB=False \
WANDB_MODE=disabled \
./sonic_launch/heft_wpc/run_sonic_wpc_rank0.sh \
++algo.config.num_learning_iterations=30000




```


输出目录/root/xuxiangling/sonic/training_checkpoints/sonic_wpc_server_001/

会产生
```text
config.yaml
meta.yaml
last.pt
model_step_002000.pt
model_step_004000.pt
……
model_step_030000.pt

```


训练开始后，另开一个 SSH终端查看 GPU

watch -n 2 nvidia-smi

