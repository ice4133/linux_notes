在最开始给雷神minipc配置的时候

串口权限不够

于是我直接把用户加入dialout组了

但是我看到imu_launch.py里面有注释说不改

没看懂


因为我自己本身就加入了dialout组，所以没有遇到权限问题
雷神minipc的用户没有加入dialout组，所以才会遇到权限问题

```bash
sudo usermod -a -G dialout $USER

# 注销

#还是不行

# 重启就行了
```
