# 装ubuntu22.04
首先拿到雷神主机

用U盘装系统，装完系统后，安装ubuntu22.04

雷神主机连接上显示屏和键盘鼠标才能安装系统

这个主机好像默认有windows

按住f10或者delete键，直接同时按吧，我忘了是哪个键了

在bios中，设置 u盘启动为第一优先级，第二优先级是硬盘启动

重启后就会从u盘启动，进入安装界面

把u盘拔掉

后面再一直点下一步就好了，安装完成后重启就进入ubuntu了

# 装vscode和git

从官网下载vscode

sudo dpkg -i code_1.79.2-1691751538_amd64.deb (按照自己的来)

安装中文插件

sudo apt install git

git --version



插件下载:
C/C++ Extension Pack
C/C++

Cmake Tools

Robot Developer Extension for ROS2

urdf //查看机器狗3d模型
Robot Developer Extension for URDF

xml
yaml


python
jupyter//可下可不下，在minipc中我没有下载


# 装vpn
https://devpn.com/combo/

进入
下载软件
并且下载两个依赖
还需要apt --broken-fix

导入订阅
# 安装toolchain
sudo apt install build-essential //会把所有都下好了，包含gcc、g++、make、cmake、gdb
等工具


# 安装ros2 humble
https://docs.ros.org/en/humble/Installation/Ubuntu-Install-Debians.html

需要vpn，即使有，也有网络不好的情况，建议多试几次
必须sudo apt update && sudo apt upgrade -y
否则apt源识别不到ros2的包

source /opt/ros/humble/setup.bash
source ~/.bashrc

ros2 run demo_nodes_cpp talker
ros2 run demo_nodes_py listener

测试成功了就可以继续了


sudo apt update
sudo apt install python3-colcon-common-extensions

# 安装python3.8

得要想个办法，最后要求是python3.8

echo $PYTHONPATH
需要把python3.8的路径加入到环境变量中

```bash
sudo apt install software-properties-common -y
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update
sudo apt install python3.8 -y
```



# 软件环境配置

从github上面拉取代码，放到工作区的src文件夹下面


# 之后请看环境配置以及移植/环境配置.md
